"https://{default}/":
    type: upstream
    upstream: "app:http"

"https://www.{default}/":
    type: redirect
    to: "https://{default}/"

# The routes of the project.
#
# Each route describes how an incoming URL is going
# to be processed by Platform.sh.

# "https://lewebsite.{default}/":
#     type: upstream
#     upstream: "lewebsite:http"

# "https://www.lewebsite.{default}/":
#     type: redirect
#     to: "https://lewebsite.{default}/"
